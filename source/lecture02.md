---
var:
  header-title: "2026-2I プログラミング1 第02回 講義資料"
  header-date: "2026年04月20日（月）3時限"
---

# 第02回 2I-プログラミング1

## 準備と復習 (定着確認) <i class="fa-solid fa-stopwatch"></i>15分

### 学びの姿勢について

現在、生成AI（コーディングエージェント）を用いれば、本科目 (プログラミングの導入教育) で扱う程度のプログラムであれば、数秒でほぼ完璧なものを生成することができます。しかし、この事実から「**ソフトウェアエンジニアはプログラミングを学ぶ必要がない**」「**プログラミングスキルは不要である**」という結論は導けません。

これは、「電卓があるから、小学校で算数を学ぶ必要はない」あるいは「[Wolfram Alpha](https://ja.wolframalpha.com/)があるから、中学・高校・大学で数学を学ぶ必要がない」とはならないのと同じです。

ソフトウェア開発は、数学とは違って、唯一の正解が存在する世界ではありません。無数の選択肢のなかから、価値があり、利用者に受け入れられるものを設計・実装していく必要があります。例えば、Todoアプリは数多く存在しますが、実際に使われ続けるものは限られています。基礎を理解せぬ人が開発したようなアプリでは、機能面、性能面、セキュリティ面において、それらの競争に参加することすら難しいのが現実です。

前回の野球の例に戻ると、競技経験のある人が監督としてチームを率いる場合と、そうでない人が指揮を執る場合とでは、大きな差が生まれることは明らかです。同様に、AI が生成したコードを適切に評価し、改善するためには、少なくともプログラミングの基礎的理解とスキルが不可欠となります。AI は強力な道具ですが、その価値を引き出せるかどうかは使い手に依存します。本科目での学びは、その力を身につけるための重要な第一歩と考えてください。

### Google Colab にアクセス

[GoogleColab](https://colab.research.google.com/?hl=ja) にアクセスして OMUID でログインしてください。

![img](figs/02/colab-01.png)

ノートブックを「**新規作成**」して `PG1-第02回講義.ipynb` に名前を変更してください。拡張子の `.ipynb` は <span class="masked">Interactive Python Notebook</span> の略でした。

- Interactive: 対話的

![img](figs/02/colab-02.png)

### テキストセルの追加

ノートブックに「**テキストセル**」を追加して、`# 準備と復習（定着確認）` を記入して、次のようなレンダリング出力が得られるようにしてください。

- テキストセルが、初期配置のコードセルよりも上部にくるように、セル右上の <i class="fa-solid fa-arrow-up"></i> アイコン使って位置替えをしてください。
- `# 準備と復習（定着確認）` の `#` はマークダウン書式の「**見出し (Header) **」を意味します。 
  - `#` と `準備と復習（定着確認）` の間に半角スペースを入れるようにしてください。
  - `#` は、半角で記入してください。

![img](figs/02/colab-03.png)

**<i class="fa-solid fa-comment-dots fa-flip-horizontal"></i>プロンプト例**

> マークダウン書式ってなんですか？「`#` はマークダウン書式の「見出し」を意味します。」と言われましたが、意味不明です。文系の高校1年生でも分かるように解説してください。

### コードセルの追加

ノートブックに「**コードセル**」を追加して、次のようにプログラムを記述してください。授業時間中は、直打ちせずにコピー＆ペーストしてください (自学するときは、原則、キーボードから直入力してください)。


```python{.numberLines caption="改行を含む文字列の出力"}
%reset -f
print('Pythonの世界へようこそ！', 'Hello, World!', sep='\n')
print('これがあなたの最初のプログラム出力です。') 
```

![img](figs/02/colab-04.png)

プロンプトを実行して結果を確認してください。

プログラムを実行するためには、対象のコードセルがアクティブな状態 (＝コードセルが選択された状態) で <span class="masked"> `Ctrl+Enter` </span> のショートカットキーを押下します。


### プログラムの理解とメンタルモデルの構築

プログラムの「コード」と「実行結果」をよく見比べて「メンタルモデルの構築」に努めてください。

- **メンタルモデル** とは <span class="masked">物事の仕組みや因果関係（こうしたら、こうなるはず）について、頭のなかに構築したイメージ</span> のことです。詳しくは[前回講義](lecture01.html#メンタルモデルの構築)を参照してください。

**<i class="fa-solid fa-comment-dots fa-flip-horizontal"></i>プロンプト例**

> 先生に「コードと実行結果を見比べてメンタルモデルを構築することが大事」と言われたのですが、正直よく分かりません。そのメンタルモデルってのを構築することが、プログラミングの学習では大事なことなんですか？

また、プログラムのなかの不明点は、コードセルの「**コードを変換**」から Gemini を呼び出して解決してください。


![img](figs/02/gemini-01.png)

**<i class="fa-solid fa-comment-dots fa-flip-horizontal"></i>プロンプト例**

> このプログラムを実行すると、文字列が3行に分けて出力されます。なぜそのような出力になるのか、プログラムと実行結果が結びつきません。特にプログラムの2行目で、括弧のなかをカンマで区切っている理由、`sep='\n'` が意味していること（なにかの指示？）が、知りたいです。文系の高校1年生でも分かるように丁寧に解説してください。

> 解説ありがとうございます。なんとなく分かったような気がします。ただ、理解した気になっただけで、実際は、全然理解できていないかもしれません。本当に理解できたかを確認するコード記述テストを作成してください。

GoogleColab から Gemini を使用すると、Gemini が **テキストセルやコードセルを追加したり、書き換えたりします**。その操作を受け入れるときは「**許可**」を押下してください。受け入れずに、元に戻したい場合は「**キャンセル**」を押下してください。

![img](figs/02/gemini-03.png)

### ノートブックの保存場所の確認


最後に、ノートブックが <span class="masked">Googleドライブ</span> の `Colab Notebooks` というフォルダのなかに保存されていることを確認してください。

## 前回の復習

前回講義で学んだことの**要点をおさらい**しておきます（少しだけ新規の内容が含まれています）。

### プログラミングの学び方について

- 学校の授業（とりわけ一斉進行型の集団教育）には、その特性・宿命として **90分の授業を1週間の間隔で実施する** という形態になります。
    - そこでの「学び」の速度は、亀の歩み。
    - そこでの「学び」の量は、スズメの涙。
  - 1週間のブランク（プログラミングをしていないこと）で失われた「感覚」や「慣れ」「記憶」を取り戻すために、授業では **復習** の時間が設けていますが、その結果として、新しい内容を学ぶために授業で使える時間は <span class="masked">70分程度</span> です。
    - **3歩進んで2歩下がること**の繰り返し。
  - 授業「**だけ**」に頼ると**約1か月**もかかる内容も、**自学なら休日の約5時間（半日）**で学べてしまいます。独学最強🚀
    - $70\ \mathrm{Min} \times 4\ 回=280\ \mathrm{Min}=4.7\ \mathrm{H}$
    - 受け身で集団教育で学ぶことの限界、マス教育の宿命です。
  - 90分授業を通年受講 (30回受講) しても、その **累積学習時間は** わずか $90\times 30= 2,700\ \mathrm{Min}=45\ \mathrm{H}$ です。
    - 毎回の授業で約90分かかる宿題が課されても、わずか **90時間** です。

### GoogleColab がフリーズしたときの対処法

GoogleColab が **フリーズしたとき** (＝処理が終わらない、操作を受付けない状態になったとき) は、[前回講義で解説済み](lecture01.html#ノートブックがフリーズしたときの対処法-必読)です。


## print関数による文字列の出力

print関数 (プリント関数) を利用した文字列の出力について学んでいきます。

### 基本形

「**文字列**」を出力するためには、***print関数*** に「**シングルクォーテーション**」または「**ダブルクォーテーション**」囲んだ **文字列リテラル** を与えます。

```python{.numberLines caption="シングルクォーテーションを利用した例"}
print('Hello Python')
```

```python{.numberLines caption="ダブルクォーテーションを利用した例"}
print("Hello Python")
```

**<i class="fa-solid fa-comment-dots fa-flip-horizontal"></i>プロンプト例**

> プログラミングの文脈で「リテラル」ってなんですか。

---

ここで「次のようなコードだったらどうなるのか？」と発想でき、**さらに手間を惜しまずに手を動かして実際に確認できる学生**は、中長期的に見てプログラミングの理解が早く、スキルも着実に向上していきます。

```python{.numberLines caption="シングルとダブルの組み合わせ (OK?NG?)"}
print("Hello Python')
```

```python{.numberLines caption="クォーテーションなし (OK?NG?)"}
print(Hello Python)
```

```python{.numberLines caption="バッククォーテーション (OK?NG?)"}
print(`Hello Python`)
```

```python{.numberLines caption="全角クォーテーション (OK?NG?)"}
print(”Hello Python”)
```

#### 本質的に大事なこと

上記のような「解説」が与えられたときに、次のような「**疑問 (＝興味関心)**」が瞬時に頭に思い浮かぶように思考回路を変えていってください。

また、その疑問に対して「**調べる**」「**試す**」「**考える**」という行動を実際にとれるよう、**マインドと行動の両面を意識して変えていくこと** が大切です。

> - `print` を `Print` にするとエディタ (コードセル上の表示) には何か変化があるの? それを実行するとどうなるの?
- `print('Hello Python')` の各所に**改行**や**スペース**を入れることはできるの? どの位置に入れてもいいの? ダメな場所もあるの? どんな規則なの?
- シングルとダブルの使い分けは? **どちらが主流**なの?
- シングル/ダブルクォーテーションで**囲まない**とエディタ (コードセル上の表示) には何か変化があるの? それを実行するとどうなるの?
- シングル/ダブルクォーテーションの囲みのなかで**日本語**は使えるの? じゃあ<span class="masked">絵文字 (🤣) </span> は使えるの? 逆に<span class="masked">使えない文字</span>はあるの?
- 文字列の途中で**改行したい**ときはどうするの?
- 出力される**文字の色を変えたい**、**文字を装飾したい** (太字、斜体、フォント変更、サイズ変更、$\mathrm{CO}_{2}$ のような下付き文字にしたい) ときはどうするの?
- シングルクォーテーションで始まって、ダブルクォーテーションで終わるとどうなるの?
- `print("I'm happy.")` のようにダブルクォーテーションで囲った文字列のなかで、シングルクォーテーションを使うことはできるの? できないなら「**I'm happy.**」と出力するためにはどうすればいいの?
- `print("The cat said, "Meow."")` のようにクォーテーションを<span class="masked">ネスト (入れ子)</span> にできるの? ネストしたい場合はどうすればいいの?
- シングルとダブルのクォーテーションの**両方を含む文章** (例えば<span class="masked">「The boy said, "That's my sister's bike."」</span>) を出力するにはどうすればいいの?


学習者としてだけでなく、**つくる側 (クリエイタ) の視点**も持ってみましょう。

- 自分のやりたいことに `print` 関数をどう活かせるか
- `print` を使ってどのような工夫や価値を生み出せるか
- 実際のアプリやサービスのなかで、同様の仕組みがどのように使われているか

こうした視点を持つことで、学習は単なる知識の習得ではなく、実践的な理解へとつながっていきます。

### エディタによる文法チェック

次のようにエディタ上 (コードセル上) に「赤線」や「青線」が表示されのは、プログラムコードに **文法ミス** があるためです。

![img](figs/02/colab-06.png)

<span class="masked">カッコの閉じ忘れがないか</span>、クォーテーションの閉じ忘れがないか、その他、文法上のミスがないかなどを注意深く確認してください。

### 文末の処理

```python{.numberLines}
%reset -f
print('Hello Python.')
print('Java? No Thanks!')
```

上記を実行すると、次のように改行され出力されます。

```
Hello Python.
Java? No Thanks!
```

ここで、次のように **文字列を改行させずに出力させたいとき** はどうしたらよいでしょうか?（また、ピリオドのあとにスペースも入れたい…）

```
Hello Python. Java? No Thanks!
```

もちろん `print('Hello Python. Java? No Thanks!')` のように「文字列をひとつにまとめてしまう方法」もありますが、あとで学習する**繰り返し処理**と組み合わせるときには、その方法は適しません。

さて、***どのようなキーワードで検索*** あるいは ***どのようなプロンプトで生成AIに質問*** すれば目的とする情報が得られるでしょうあか? 実際に試してみてください。周囲のクラスメイトとも情報交換してみてください。

### 変数の埋め込み

ここでは、文字列のなかに ***変数*** を埋め込んで出力する方法を学びます。

<div class="note type-tips">
**変数とは**

「**変数**」とは、プログラムのなかでデータ (=文字列や数値など) を一時的に保存したり、取り扱ったりするために使われるものです。変数を使うことで、**データに名前をつけ**、後からそのデータを**参照**したり (=読み出したり)、**更新**したり (=別の値を上書きしたり) できます。
</div>


```python{.numberLines}
%reset -f
print('生まれは堺、育ちも堺、就職先も堺です。両親も堺に住んでいます。')
```

上記の文字列には「**堺**」という文字列が **4回登場** します。この文章のなかで「堺」を「寝屋川市」や「門真」に変更する可能性があるときは、あらかじめ、次のように ***変数*** を利用してプログラムを構成しておきます。

```python{.numberLines caption="変数の活用"}
%reset -f
city = '堺'
print('生まれは' + city + 'で、育ちも' + city + '、就職先も' 
      + city + 'です。両親も' + city + 'に住んでいます。')
```

ここで `city` が「**変数**」というものになります。上記の **第01行目** は、`=` の記号を使っていますが <span class="masked">数学的におけるイコールとは意味が異なり</span>、次のような意味を持ちます。

- `city` という**変数** (容器) に「堺」という文字列を「**格納する**」「**代入する**」。

そのため `10=a` は **SyntaxError** (=文法誤り) となり、また `a=b` と `b=a` では <span class="masked">実行される処理がまったく違います</span>。なお、C言語などでは、型宣言 (明示) が必要になりますが、Pythonでは不要です。

<div class="note type-senior">
**中級者向け: 厳密には「代入」ではなく「バインド(束縛)」**

Pythonにおいて `city='堺'` という文は **厳密にいえば**「変数 `city` を『堺』という文字列 (オブジェクト) にバインド (束縛)する」という意味になります。詳しくは [小山高専・技術支援室](https://www.oyama-ct.ac.jp/tso/workshop/python/python03.html) を参照してください。

</div>

#### 演習① <i class="fa-solid fa-stopwatch"></i>15分

- Google Colab. のノートブック上で、上記のプログラムを実行・確認してください。
- 同様に変数を利用して「**人民による人民のための人民の政治**」という文字列を出力してください (新たにコードセルを追加して記述・実行してください)。
- print関数内の `city` を `'city'` に変更し、エディタ上の表示 (=コードセル内の表示) がどのように変化するか確認してください。また、実行するとどうなるか確認せよ。確認後は、変更を元に戻しておいてください。
- print関数内の `city` を**1箇所だけ** `ciyt` に変更し、エディタ上の表示がどのように変化するか確認してください。また、実行するとどうなるか確認せよ。確認後は、変更を元に戻しておいてください。
- このような変数の埋め込みは「**ゲームでは、どのようなところに利用されているか**」をできるだけ多く考え、周囲の学生と意見交換してください。

上記をすべて授業時間内に実行することは難しいので、実施できなかった分は、**宿題として放課後や自宅などで次回までに取り組んでおいてください**。以降の演習についても同様です。

<div class="note type-senior">
**中級者向け: 意外な落とし穴**

次のプログラムの実行結果を予想し、実際に実行して確認してください。また、なぜそのような結果となるかを探ってください。

```python{.numberLines caption="ときには想像と違うことが起きる"}
%reset -f
a = 'あいうえお'    # 変数名は半角「a」
ａ = 'かきくけこ'   # 変数名は全角「ａ」
print(a)  # 引数には半角「a」
```
</div>

#### おまけ: 文字列に「色」をつける

Goole Colab. 環境では **ANSIエスケープシーケンス** という特殊な文字列を利用して、出力文字に色を付けることができます。サンプルを以下に示します。詳しくはウェブ検索や生成AIを利用して調べてみてください。

```python{.numberLines caption="ANSIエスケープシーケンスを利用して文字色を変える"}
%reset -f
RED = '\033[31m'
GREEN = '\033[32m'
RESET = '\033[0m'
print(RED + '赤いキツネ' + RESET + 'と' + GREEN + '緑のタヌキ' + RESET )
```

同様に <span class="masked">太字</span> や <span class="masked">下線</span> などの装飾をすることもできます。また、文字の背景色をかえることもできます。

ただし、**実行環境によっては対応していないこともあるので注意**してください。その場合、ANSIエスケープシーケンスの文字がそのまま出力されます (文字化けを起こしているように見えます)。

### 変数の埋め込み ( f文字列 )

Python のバージョン 3.6 以上では ***f文字列*** (**フォーマット済み文字列リテラル**) という機能が利用できます。

<div class="note type-tips">
**フォーマットとは**

プログラムの文字列処理における「**フォーマット**」とは、文字列のなかに**変数を埋め込んだり**、特定の形式に従って**整形したり**することを指します (この文脈では「初期化」の意味ではありません)。

ここでの「**整形**」とは (太字や斜体などの処理ではなく)、数値の `20` を `20.00` のように指定の小数点位までの表示したり、`13` を `1101` のように **2進数表記** にしたりすることを意味します。

</div>

 ***f文字列*** の機能で文字列を埋め込むには、文字列の先頭に `f` を付け、文字列内部に中括弧 (波括弧、にょろ括弧) で囲った内部に変数を記述します。

```python{.numberLines caption="f文字列の利用例"}
city = '堺'
print(f'生まれは{city}、育ちも{city}、就職先も{city}です。両親も{city}に住んでいます。')
```

#### 演習② <i class="fa-solid fa-stopwatch"></i>10分

- Google Colab のノートブック上で、上記のプログラムを実行・確認してください。
- 出力される文字列の「**堺**」を「**枚方**」に変更するように変更して、実行確認をしてください。
- 文字列の先頭の `f` を <span class="masked">大文字</span> にするとどうなるか確認してください。。
- 文字列の先頭の `f` を <span class="masked">削除</span> するとどうなるか確認してください。
- f文字列のなかで、出力文字として `{}` を使用したい場合、例えば `{必読} 山田さま限定の割引情報` という**波括弧を含んだ文字列**を出力したい場合は、どれのようにすれば良いか調べて確認してください。

#### 補足

<div class="note type-tips">
**Pythonのバージョンを調べるには**

Pythonのバージョンは、***コマンドプロンプト*** から、次のコマンドを入力して確認できます。ハイフンの次につける文字を、一般に「**オプション**」と呼びます。大文字・小文字が区別されるので注意してください。

```
python -V
```

**GoogleColab環境** (Jupyter環境) では、次のようにコードセルのなかで先頭に `!` を付けてコマンドを実行します。`!` を付けないと Pythonのコードとして認識されてしまいます( `!` を付けることで仮想マシンのターミナルに直接コマンドを与えることができます )。

```
!python -V
```

GoogleColab のPythonバージョンを把握しておきましょう。なお、Pythonのリリース情報は[公式ページ](https://www.python.org/downloads/)から確認できます。

![img](figs/02/colab-07.png)

</div>


<div class="note type-senior">
**中級者向け: 仮想マシンのスペック確認**

次のコマンドを GoogleColab のコードセルで実行すると、GoogleColab が動作している「仮想PCのスペック」を取得することができます。

```
!cat /etc/os-release
!cat /proc/cpuinfo
!cat /proc/1eminfo
!nvidia-smi
```

なお、`!nvidia-smi` は、GPUの割り当てをしている場合のみ有効です。メニューの「ランタイム」-「ランタイムのタイプを変更」からGPUの割り当てが可能です。

![img](figs/02/colab-08.png)

</div>

## 乱数処理との組み合わせ

**文字列処理(出力)**に ***乱数処理*** を組み合わせると、それだけで面白いプログラムを作成することができます。

```python{.numberLines caption="予告ジェネレーター"}
%reset -f
import random

x1 = random.choice(['美人','アキバ系','婚活','引きこもり','還暦','リア充'])
x2 = random.choice(['高専生','YouTuber','家政婦','女将','教師','教祖'])
y1 = random.choice(['美人','アキバ系','婚活','引きこもり','還暦','リア充'])
y2 = random.choice(['高専生','YouTuber','家政婦','女将','教師','教祖'])

print(f'来週の火曜サスペンス劇場は', end='')
print(f'「{x1}{x2}は見たッ! {y1}{y2}殺人事件ッ!!」です。ご期待ください。')
```

まずは、プログラムをコードセルに貼り付けて実行してみましょう ( ***試す*** )。また、実行するたびに <span class="masked">実行結果が変わること</span> を確認しましょう。

次に「なぜ、このプログラムから、そのような実行結果が得られるのか」について ***考えて*** みましょう。また、ある程度の関係性がみえてきたら、その解釈が適切なのか、実際に検証してみましょう ( ***試す*** )。

例えば、<span class="masked">「陽キャ」「陰キャ」という修飾語を加えたい場合</span>は、どのように書きかえればよいか、などを書き換えて、実行してみましょう。

また「必要と思われる処理」を**あえてコメントアウトして**<span class="masked">どのような結果になるか</span>を確認してみましょう。また、***「必要ないのと思う処理」***があるなら、実際にコメントアウトしてみましょう (実際、必要ない処理なのかもしれまん)。

  - コメントアウトとアンコメントは `Ctrl+/` のショートカットキーで実行します。多用するので必ず覚えてください。

また、そのうえで分からないことについてウェブで ***調べたり*** 、先生やクラスメイトに尋ねてみましょう。**何が分からないかを自分が認識できていないときに質問しても、学習効果は薄いです** (質問相手の時間を無駄に奪うだけですので、注意してください。)。

### リファクタリング

***リファクタリング*** とは、**プログラムの外面的な振る舞いを変更せずに、内部のコード構造を改良・最適化する行為**です。上記プログラムは、少なくとも次のようにリファクタリングできます。

リファクタリングは、可読性、保守性、拡張性など様々な観点で行われるため、一概に、このようにすべきという答えはありません。

```python{.numberLines caption="予告ジェネレーター（リファクタリング）"}
%reset -f
import random

word1 = ['美人','アキバ系','婚活','引きこもり','還暦','リア充']
word2 = ['高専生','YouTuber','家政婦','女将','教師','教祖']

x1 = random.choice(word1)
x2 = random.choice(word2)
y1 = random.choice(word1)
y2 = random.choice(word2)
print(f'次回の火曜サスペンス劇場は', end='')
print(f'「{x1}{x2}は見た! {y1}{y2}殺人事件ッ!!」です。ご期待ください。')
```

さらに、次のようにリファクタリングできます。

```python{.numberLines caption="予告ジェネレーター（さらにリファクタリング）"}
%reset -f
import random as r

word1 = ['美人','アキバ系','婚活','引きこもり','還暦','リア充']
word2 = ['高専生','YouTuber','家政婦','女将','教師','教祖']

print(f'次回の火曜サスペンス劇場は', end='')
print(f'「{r.choice(word1)}{r.choice(word2)}は見た!', end='')
print(f'{r.choice(word1)}{r.choice(word2)}殺人事件ッ!!」です。ご期待ください。')
```

#### 演習③

- 上記のプログラムを参考に、主に**乱数処理**と**文字列出力**を組み合わせて「高専2年生が楽しめるアプリ」を設計・実装してください。

## 宿題

- テキストを再読し「演習①」から「演習③」に取り組んでください。
- 次回、**小テスト** (ペーパーテスト) を実施します。今回の授業で扱った文字列出力に関するプログラムを正しく書けるようになっておいてください。小テストの形式は [こちら](https://omunet.sharepoint.com/:f:/r/sites/S_1_2026/Shared%20Documents/General/%E5%8F%82%E8%80%83/%E5%B0%8F%E3%83%86%E3%82%B9%E3%83%88%E3%81%AE%E5%BD%A2%E5%BC%8F?csf=1&web=1&e=OMbs5O) を参考にしてください。
    - 「f文字列」「変数」「print関数の `sep` や `end` などのオプション」を含みます。
    - 「乱数」「ANSIエスケープシーケンス」は含みません。
    - 未使用の変数を参照した場合や、クォーテーションや括弧の閉じ忘れがあった場合、変数や関数名に誤りがあった場合、不適切なインデントがあった場合、全角スペースや全角クォーテーションが使われた場合などに、**どのようなエラーとメッセージが表示されるのか** も把握しておいてください。
      - `SyntaxError: unterminated string literal`
      - `SyntaxError: incomplete input`
      - `SyntaxError: invalid non-printable character U+3000`
      - `NameError: name 'xxxx' is not defined`
      - `IndentationError: unexpected indent`
    - 筆記用具を持参してください。また、遅刻しないように注意してください。 

なお、当然のことですが、第1回の小テストが年間を通して最も易しい内容となります。ここで満点を取るための行動と結果が伴わなければ、以降の小テストはかなり厳しくなると考えられます。

